# Session: 2026-07-23 Morning

**Started**: 2026-07-23 09:36:23 IST
**Focus Task**: T22
**Status**: 🔄 ACTIVE

## Work Done

Working on T22: Recorded the forensic process-identification plan after the process-monitor repo audit and live macOS process investigations.

Key decision: the repository should keep a cross-platform architecture, but the forensic process attribution layer must use explicit platform adapters.

Portable core:
- Interval CPU profiling from cumulative CPU time deltas.
- Stable process identity: PID, PPID, user, executable path, command line, start time, state, memory, and cumulative CPU time.
- Parent-chain reconstruction, historical storage, CLI reports, and dashboard views.

macOS-first adapter:
- `launchctl` for launchd label/domain/state/exit-status attribution.
- LaunchAgent/LaunchDaemon plist scan and missing-target validation.
- `lsof` for open files and listener ports.
- `sample` and `fs_usage` for on-demand stack and IO traces.
- Optional code-signature, bundle ID, and package receipt checks.

Later Linux adapter:
- `/proc`, `systemctl`, unit files, cgroups, `ss`/`lsof`, optional `strace`/`perf`/eBPF, and package ownership.

Later Windows adapter:
- WMI/PowerShell, Windows Services, Scheduled Tasks, ETW/Event Logs, and handle/socket inspection.

Implemented first T22 code slice:
- Added `/api/process-forensics` for live process identity, parent chain, recent CPU summary, findings, listener ports, and open files.
- Added `/api/process-cpu-profile` for bounded CPU interval profiling by cumulative CPU-time deltas.
- Added `/api/analysis/troublesome-processes` as the first investigation queue.
- Added process modal forensic sections and Analysis actions for Troublesome Processes and 5-second CPU Profile.
- Fixed current UI/API issues in Recent Drain Events, Sleep ranges, Analysis default state, Reports default state, duplicated Process Consistency renderer, and report rendering.
- Added `HOST` support so the dashboard can be verified on `127.0.0.1` without changing the default network binding.

Validation:
- `npm run build` passed.
- `git diff --check` passed.
- Browser smoke test passed on `HOST=127.0.0.1 PORT=3457 ./node_modules/.bin/tsx src/web/server.ts`.

Remaining:
- Persist process identity/provenance in SQLite.
- Add full macOS `launchctl` mapping, LaunchAgent/LaunchDaemon plist audit, missing-target detection, `sample`, and `fs_usage`.
- Add proper Linux and Windows adapter implementations later.
